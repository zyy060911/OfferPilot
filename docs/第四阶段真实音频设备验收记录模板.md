# 第四阶段真实音频设备验收记录

> 目的：验证半双工持续采集、静音终点判断、ASR 聚合和自动提交。
> 禁止记录真实候选人的姓名、联系方式、完整回答或原始音频；测试时使用无敏感信息的固定示例句。

## 测试批次

- 日期：
- 测试人员：
- Git 分支与提交：
- 前端启动方式及调试开关：
- 后端版本：
- 数字人版本：
- 操作系统：
- 浏览器及完整版本：
- 网络环境：

## 设备能力记录

- 输入设备类型/型号：笔记本内置 / 有线耳机 / 蓝牙耳机 / 外接麦克风
- 输出设备类型/型号：
- 浏览器报告的 `echoCancellation`：true / false / null
- 浏览器报告的 `noiseSuppression`：true / false / null
- 浏览器报告的 `autoGainControl`：true / false / null
- 实际采样率：
- 实际声道数：
- 备注（不要记录设备唯一 ID）：

## 单项用例记录

复制本节，为每个“设备 × 环境 × 讲话方式”分别填写。

### 用例：__________

- 输入设备：
- 输出设备：
- 环境：安静房间 / 风扇或空调 / 键盘输入 / 其他
- 讲话方式：正常 / 较远距离 / 小声 / 快速 / 长时间思考停顿
- 固定测试内容编号（不要写完整敏感回答）：
- 阻断期 RMS 摘要（RMS/peak/时长）：
- 正常讲话 RMS 范围：
- 当前实现噪声基线：无（固定 RMS 阈值实现）/ 其他：
- 是否首字丢失：否 / 是，现象：
- 是否把数字人声音识别进答案：否 / 是，片段序号：
- 是否提前进入 `POSSIBLE_END`：否 / 是，时间点：
- 是否错误自动提交：否 / 是，触发条件：
- 是否长时间不提交：否 / 是，静音时长：
- 最后可靠语音结束到 `POSSIBLE_END`：____ ms
- `POSSIBLE_END` 到提交开始：____ ms
- 最后可靠语音结束到提交开始总计：____ ms
- ASR 是否丢段：否 / 是，segment sequence：
- ASR 是否乱序：否 / 是；最终文本顺序是否正确：
- ASR 是否重复：否 / 是，segment sequence：
- 28 秒切片是否保持同一 `answerId`：
- 数字人播报及 playback guard 期间门控是否保持阻断：
- ASR 失败/超时后的重试结果：
- 最终结论：通过 / 有条件通过 / 不通过
- 缺陷编号与复现步骤：

## 最低覆盖矩阵

| 设备 | 安静 | 风扇/空调 | 键盘 | 远距离 | 小声 | 快速 | 长停顿 | 结论 |
|---|---|---|---|---|---|---|---|---|
| 笔记本内置麦克风和扬声器 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ | |
| 有线耳机 | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ | |
| 蓝牙耳机（如可用） | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ | |
| 外接麦克风（如可用） | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ | |

## 批次结论

- 自动提交准确率（通过用例/总用例）：
- 提前提交次数：
- 超时不提交次数：
- 数字人回声进入 ASR 次数：
- 首字丢失次数：
- 需要阻断发布的问题：
- 是否建议进入下一阶段：是 / 否
- 审核人：
